import * as navbarStyles from "./navbar.scss";
import * as globalStyles from "./global.scss";
import * as dashboardStyles from "./dashboard.scss";

const styles = navbarStyles + globalStyles + dashboardStyles;

export default styles;
